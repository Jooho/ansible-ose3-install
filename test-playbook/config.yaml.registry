
 - name: Test for each role
   hosts: master1.example.com
   gather_facts: false
   
   pre_tasks:
   - name: set concurrent path
     local_action: command pwd
     register: pwd
     tags: pre_tasks

   - name: set_facts - pwd_path
     set_fact:
        pwd_path: "{{pwd.stdout}}"
     tags: pre_tasks

   roles:
      #    - { role: ose_nfs, nfs_mount_point: /exports/nfs}
      - { role: create_ose_registry, attach_nfs_volume_to_registry: "true", nfs_server: "infra.example.com" ,nfs_registry_mount_point: "/exports/nfs/registry"}
